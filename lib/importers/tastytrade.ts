import { ImportResult } from "./index";

// TastyTrade CSV parser (needs real sample to flesh out)
export function parse(_csvText: string): ImportResult {
  // TODO: implement TastyTrade-specific parsing once sample CSV is provided
  return { fills: [], skipped: [] };
}
