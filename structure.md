Application Folder Structure

trade-journal/
├── CLAUDE.md                      ← project context (the important one)
├── .env.local                     ← API keys, gitignored
├── .env.example                   ← committed template, no secrets
├── .gitignore
├── README.md
├── package.json
├── tsconfig.json
├── next.config.ts
├── tailwind.config.ts
│
├── .claude/
│   └── skills/
│       └── supabase-queries/
│           └── SKILL.md           ← optional, your common query patterns
│
├── supabase/
│   ├── migrations/                ← SQL schema changes, version controlled
│   └── seed.sql                   ← optional sample trades
│
├── src/
│   ├── app/
│   │   ├── layout.tsx
│   │   ├── page.tsx               ← dashboard
│   │   ├── trades/
│   │   │   ├── page.tsx           ← trade list
│   │   │   ├── new/page.tsx       ← entry form
│   │   │   └── [id]/page.tsx      ← single trade detail
│   │   └── api/
│   │       └── quotes/route.ts    ← proxy to Polygon (keeps key server-side)
│   │
│   ├── components/
│   │   ├── ui/                    ← shadcn components land here
│   │   ├── trade-form.tsx
│   │   ├── trade-table.tsx
│   │   └── stats-card.tsx
│   │
│   ├── lib/
│   │   ├── supabase/
│   │   │   ├── client.ts          ← browser client
│   │   │   └── server.ts          ← server client
│   │   ├── calculations.ts        ← P&L, R-multiple, win rate, etc.
│   │   ├── quotes.ts              ← Polygon wrapper
│   │   └── schemas.ts             ← Zod schemas for Trade, Quote, etc.
│   │
│   └── types/
│       └── database.ts            ← generated by Supabase CLI